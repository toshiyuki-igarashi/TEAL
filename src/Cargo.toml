[workspace]
members = [
  "teald",
  "teal_policy_engine",
  "pam_teal",
]  
resolver = "2"

[profile.release]
opt-level = 3         # 最高レベルの最適化 (-O3相当)
lto = "fat"           # リンク時最適化 (バイナリ全体を跨いだ最適化)
codegen-units = 1     # 依存関係の最適化精度を最大化
panic = "abort"       # エラー処理を簡略化して高速化
